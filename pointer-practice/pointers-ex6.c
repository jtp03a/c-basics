#include <stdio.h>
#include <stdlib.h>

int main(){
   char *str = (char*)calloc(100, sizeof(char));
   printf("Input a string");
   scanf("%s", str);

   for (int i=0; str[i] != '\0'; i++) {
      
   }



   return 0;
}
